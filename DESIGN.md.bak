---
version: alpha
name: PJPC 安亲班管理系统
description: 温馨、专业的安亲班（课后托管）教育管理解决方案。暖琥珀色系，圆润亲和，板块清晰。
colors:
  background: "#FAF7F3"
  foreground: "#514A2E"
  primary: "#E8C86A"
  "primary-foreground": "#514A2E"
  secondary: "#EFEBE1"
  "secondary-foreground": "#514A2E"
  muted: "#EFEBE1"
  "muted-foreground": "#7A6B5A"
  accent: "#E8C86A"
  "accent-foreground": "#514A2E"
  destructive: "#DC2626"
  "destructive-foreground": "#FAFAFA"
  border: "#C4BBA8"
  input: "#C4BBA8"
  ring: "#E8C86A"
  "sidebar-background": "#EFEBE1"
  "sidebar-foreground": "#6B5D4D"
  "sidebar-primary": "#E8C86A"
  "sidebar-primary-foreground": "#514A2E"
  "sidebar-accent": "#F5EDD4"
  "sidebar-accent-foreground": "#6B5D4D"
  "sidebar-border": "#C4BBA8"
  "sidebar-ring": "#E8C86A"
  "amber-50": "#FFFBEB"
  "amber-100": "#FEF3C7"
  "amber-200": "#FDE68A"
  "amber-400": "#FBBF24"
  "amber-500": "#F59E0B"
  "amber-600": "#D97706"
  "amber-700": "#B45309"
  "amber-800": "#92400E"
  "amber-900": "#78350F"
  "chart-1": "#E8C86A"
  "chart-2": "#8A6A38"
  "chart-3": "#5A9E6F"
  "chart-4": "#514A2E"
  "chart-5": "#C96A3D"
typography:
  h1:
    fontFamily: Nunito
    fontSize: 1.875rem
    fontWeight: 700
    lineHeight: 1.2
    letterSpacing: "-0.01em"
  h2:
    fontFamily: Nunito
    fontSize: 1.5rem
    fontWeight: 700
    lineHeight: 1.25
  h3:
    fontFamily: Nunito
    fontSize: 1.125rem
    fontWeight: 600
    lineHeight: 1.3
  "body-lg":
    fontFamily: Inter
    fontSize: 1rem
    fontWeight: 400
    lineHeight: 1.6
  "body-md":
    fontFamily: Inter
    fontSize: 0.875rem
    fontWeight: 400
    lineHeight: 1.5
  "body-sm":
    fontFamily: Inter
    fontSize: 0.75rem
    fontWeight: 400
    lineHeight: 1.4
  "label":
    fontFamily: Nunito
    fontSize: 0.75rem
    fontWeight: 600
    lineHeight: 1.4
    letterSpacing: "0.05em"
  "stat-number":
    fontFamily: Nunito
    fontSize: 1.875rem
    fontWeight: 700
    lineHeight: 1.1
    letterSpacing: "-0.02em"
  "stat-label":
    fontFamily: Inter
    fontSize: 0.875rem
    fontWeight: 500
    lineHeight: 1.4
rounded:
  sm: 4px
  md: 8px
  lg: 12px
  xl: 16px
  "2xl": 20px
  full: 9999px
spacing:
  xs: 4px
  sm: 8px
  md: 16px
  lg: 24px
  xl: 32px
  "2xl": 48px
components:
  "page-section":
    backgroundColor: "{colors.background}"
    padding: "{spacing.xl}"
    rounded: "{rounded.lg}"
  "page-section-alt":
    backgroundColor: "{colors.secondary}"
    padding: "{spacing.xl}"
    rounded: "{rounded.lg}"
  "page-section-cta":
    backgroundColor: "#514A2E"
    textColor: "#FFFFFF"
    padding: "{spacing.xl}"
    rounded: "{rounded.lg}"
  "card-default":
    backgroundColor: "#FFFFFF"
    rounded: "{rounded.lg}"
    border: "1px solid {colors.border}"
    shadow: "0 1px 3px 0 rgba(0,0,0,0.06)"
    padding: "{spacing.lg}"
  "card-hover":
    backgroundColor: "#FFFFFF"
    rounded: "{rounded.lg}"
    border: "1px solid {colors.primary}"
    shadow: "0 4px 12px rgba(232,200,106,0.15)"
  "card-pastel-amber":
    backgroundColor: "{colors.amber-50}"
    rounded: "{rounded.lg}"
    border: "1px solid {colors.amber-200}"
    padding: "{spacing.lg}"
  "card-pastel-cream":
    backgroundColor: "#FEFCE8"
    rounded: "{rounded.lg}"
    border: "1px solid #FDE68A"
    padding: "{spacing.lg}"
  "card-stat":
    backgroundColor: "#FFFFFF"
    rounded: "{rounded.lg}"
    border: "1px solid {colors.border}"
    padding: "{spacing.md} {spacing.lg}"
  "button-primary":
    backgroundColor: "{colors.amber-600}"
    textColor: "#FFFFFF"
    rounded: "{rounded.md}"
    padding: "8px 16px"
    fontFamily: Nunito
    fontWeight: 600
    fontSize: 0.875rem
  "button-primary-hover":
    backgroundColor: "{colors.amber-700}"
    textColor: "#FFFFFF"
    rounded: "{rounded.md}"
  "button-secondary":
    backgroundColor: "{colors.secondary}"
    textColor: "{colors.foreground}"
    rounded: "{rounded.md}"
    padding: "8px 16px"
    border: "1px solid {colors.border}"
    fontFamily: Nunito
    fontWeight: 500
    fontSize: 0.875rem
  "button-ghost":
    backgroundColor: "transparent"
    textColor: "{colors.amber-700}"
    rounded: "{rounded.md}"
    padding: "8px 16px"
    fontWeight: 500
    fontSize: 0.875rem
  "button-action":
    backgroundColor: "#514A2E"
    textColor: "{colors.primary}"
    rounded: "{rounded.md}"
    padding: "10px 20px"
    fontFamily: Nunito
    fontWeight: 600
  "input-field":
    backgroundColor: "#FFFFFF"
    border: "1px solid {colors.input}"
    rounded: "{rounded.md}"
    padding: "8px 12px"
    fontFamily: Inter
    fontSize: 0.875rem
  "input-field-focus":
    border: "2px solid {colors.ring}"
    shadow: "0 0 0 3px rgba(232,200,106,0.2)"
  "sidebar-item":
    backgroundColor: "transparent"
    textColor: "{colors.sidebar-foreground}"
    rounded: "{rounded.md}"
    padding: "8px 12px"
    fontFamily: Nunito
    fontWeight: 500
  "sidebar-item-active":
    backgroundColor: "{colors.sidebar-accent}"
    textColor: "{colors.sidebar-accent-foreground}"
    rounded: "{rounded.md}"
    borderLeft: "3px solid {colors.primary}"
  "badge-approved":
    backgroundColor: "#D1FAE5"
    textColor: "#065F46"
    rounded: "{rounded.full}"
    padding: "2px 10px"
    fontFamily: Nunito
    fontWeight: 600
    fontSize: 0.75rem
  "badge-pending":
    backgroundColor: "{colors.amber-100}"
    textColor: "{colors.amber-800}"
    rounded: "{rounded.full}"
    padding: "2px 10px"
    fontFamily: Nunito
    fontWeight: 600
    fontSize: 0.75rem
  "badge-suspended":
    backgroundColor: "#FEE2E2"
    textColor: "#991B1B"
    rounded: "{rounded.full}"
    padding: "2px 10px"
    fontFamily: Nunito
    fontWeight: 600
    fontSize: 0.75rem
  "badge-paid":
    backgroundColor: "#D1FAE5"
    textColor: "#065F46"
    rounded: "{rounded.full}"
    padding: "2px 10px"
    fontFamily: Nunito
    fontWeight: 600
    fontSize: 0.75rem
  "badge-overdue":
    backgroundColor: "#FEE2E2"
    textColor: "#991B1B"
    rounded: "{rounded.full}"
    padding: "2px 10px"
    fontFamily: Nunito
    fontWeight: 600
    fontSize: 0.75rem
  "badge-partial":
    backgroundColor: "{colors.amber-100}"
    textColor: "{colors.amber-700}"
    rounded: "{rounded.full}"
    padding: "2px 10px"
    fontFamily: Nunito
    fontWeight: 600
    fontSize: 0.75rem
  "data-table-header":
    backgroundColor: "{colors.secondary}"
    textColor: "{colors.foreground}"
    fontWeight: 600
    fontSize: 0.75rem
    letterSpacing: "0.05em"
    textTransform: uppercase
  "data-table-row":
    backgroundColor: "#FFFFFF"
    borderBottom: "1px solid {colors.border}"
  "data-table-row-hover":
    backgroundColor: "{colors.amber-50}"
  "tab-default":
    textColor: "{colors.muted-foreground}"
    fontWeight: 500
    padding: "8px 16px"
  "tab-active":
    textColor: "{colors.foreground}"
    fontWeight: 600
    borderBottom: "2px solid {colors.primary}"
  "login-card":
    backgroundColor: "#FFFFFF"
    rounded: "{rounded.lg}"
    border: "1px solid {colors.border}"
    shadow: "0 10px 25px rgba(232,200,106,0.12)"
  "login-gradient":
    backgroundImage: "linear-gradient(to bottom right, {colors.amber-50}, #FEF9C3, #FFEDD5)"
---

## Overview

PJPC 是一个面向马来西亚课后托管中心（安亲班）的全功能教育管理平台。设计语言以 **温暖亲和、板块清晰、一看就懂** 为核心。

- **品牌调性**：暖金色琥珀系，传递温馨、信任和成长的氛围
- **结构清晰**：用背景色块交替区分不同功能板块（学生/教师/财务/考勤）
- **数据导向**：大量表格和统计数据，需保持视觉轻盈不拥挤
- **目标用户**：安亲班老板、教师、家长，年龄跨度大，界面需简洁直观

## Colors

主色调 **暖琥珀色（#E8C86A / amber-400~600）**，营造温暖信任感。禁止使用冷蓝色系。

### 品牌色家族

```
amber-50  #FFFBEB  → 页面背景 / 浅色卡片
amber-100 #FEF3C7  → 待处理 badge / 悬停背景
amber-200 #FDE68A  → 卡片边框 / 过去式卡片
amber-400 #FBBF24  → 强调图标 / 亮点
amber-500 #F59E0B  → 主按钮 hover
amber-600 #D97706  → 按钮主色
amber-700 #B45309  → 按钮 hover / 辅助文字
amber-800 #92400E  → 标题 / 强文字
amber-900 #78350F  → 最深点缀色
```

### 语义色

| 场景 | 背景 | 文字 | 用途 |
|------|------|------|------|
| ✅ 已批准 / 已付款 | `#D1FAE5` | `#065F46` | 绿色成功状态 |
| ⏳ 待审核 / 部分付款 | `amber-100 #FEF3C7` | `amber-800` | 黄色等待状态 |
| ❌ 已暂停 / 逾期 | `#FEE2E2` | `#991B1B` | 红色异常状态 |

## Typography

双字体体系：**Nunito**（标题、标签、数字） + **Inter**（正文、表格）

### 标题层级

| 层级 | 字体 | 大小 | 字重 | 行高 | 用途 |
|------|------|------|------|------|------|
| h1 | Nunito | 30px (1.875rem) | 700 | 1.2 | 页面大标题 |
| h2 | Nunito | 24px (1.5rem) | 700 | 1.25 | 板块标题 |
| h3 | Nunito | 18px (1.125rem) | 600 | 1.3 | 卡片标题 |
| stat-number | Nunito | 30px (1.875rem) | 700 | 1.1 | 统计数字 |
| stat-label | Inter | 14px (0.875rem) | 500 | 1.4 | 统计说明 |

### 正文层级

| 层级 | 字体 | 大小 | 字重 | 行高 | 用途 |
|------|------|------|------|------|------|
| body-lg | Inter | 16px (1rem) | 400 | 1.6 | 正文大 |
| body-md | Inter | 14px (0.875rem) | 400 | 1.5 | 表格/表单 |
| body-sm | Inter | 12px (0.75rem) | 400 | 1.4 | 辅助文字 |
| label | Nunito | 12px (0.75rem) | 600 | 1.4 | 标签/表头（大写） |

## Layout & Spacing

### 页面结构

```
┌─────────────────────────────────┐
│  Sidebar (w-64)                 │
│  bg-amber-50/80                 │      ← 侧边栏：主菜单导航
│                                 │
│  ┌───────────────────────────┐  │
│  │ 📊 仪表板                  │  │
│  │ ┌─────┐ ┌─────┐ ┌─────┐ │  │      ← 统计卡片行
│  │ │学生数│ │费用 │ │教师数│ │  │     3 列网格
│  │ └─────┘ └─────┘ └─────┘ │  │
│  │                           │  │
│  │ ┌── 背景色交替 ──────────┐ │  │      ← 功能板块用不同
│  │ │ 📋 最近活动             │ │  │     背景色区分（Airtable
│  │ │ ...                     │ │  │     风格：白→奶油交替）
│  │ └────────────────────────┘ │  │
│  └───────────────────────────┘  │
└─────────────────────────────────┘
```

### 板块交替规则

相邻的功能板块使用不同背景色，让用户一眼看出区域切换：

| 板块 | 背景色 | 用途 |
|------|--------|------|
| Section A | `bg-white` | 主要内容区 |
| Section B | `bg-amber-50/80` | 次要内容 / 统计 |
| Section C | `bg-[#514A2E] text-white` | CTA / 强调区 |
| 卡片网格 | `bg-white grid grid-cols-3` | 功能卡片 |

### 响应式断点

| 断点 | 宽度 | 变化 |
|------|------|------|
| Mobile | < 768px | 单列；侧边栏→hamburger；表格滚动 |
| Tablet | 768–1024px | 2列网格；导航紧凑 |
| Desktop | ≥ 1024px | 3-4列网格；全导航 |

## Shapes

| 元素 | 圆角 | 说明 |
|------|------|------|
| 内容卡片 | `rounded-xl` (12px) | 默认卡片 |
| 按钮 | `rounded-lg` (8px) | 所有按钮 |
| 输入框 | `rounded-lg` (8px) | 表单字段 |
| Badge | `rounded-full` (9999px) | 状态标签 |
| 头像 | `rounded-full` | 用户/学生头像 |
| 统计卡片 | `rounded-xl` (12px) | 仪表板统计 |
| 弹窗 | `rounded-2xl` (16px) | Modal/Dialog |

## Elevation & Depth

| 层级 | 阴影 | 用途 |
|------|------|------|
| Level 0 | `shadow-none` | 页面背景卡片 |
| Level 1 | `shadow-sm` (0 1px 2px rgba(0,0,0,0.05)) | 默认卡片 |
| Level 2 | `shadow-md` (0 4px 6px rgba(0,0,0,0.07)) | 悬停状态 |
| Level 3 | `shadow-lg` (0 10px 15px rgba(0,0,0,0.1)) | 下拉 / 弹窗 |
| Level 4 | `shadow-xl shadow-amber-200/20` | 登录卡片 / 强调 |

## Motion & Animation

设计原则：**动效是功能的影子，不是主角。** 所有动画必须服务于可用性，不能为了动而动。

### 时间 & 缓动

| Token | 时长 | 缓动 | 用途 |
|-------|------|------|------|
| `duration-fast` | 150ms | `ease-out` | 悬停态、按下态、颜色变化 |
| `duration-normal` | 200ms | `ease-out` | 面板展开/收起、淡入淡出 |
| `duration-slow` | 300ms | `ease-in-out` | 页面入场、弹窗 |
| `ease-out` | — | `cubic-bezier(0.16, 1, 0.3, 1)` | 所有出场动画（默认） |
| `ease-in-out` | — | `cubic-bezier(0.65, 0, 0.35, 1)` | 状态切换动画 |

### 组件动效清单

| 组件 | 触发 | 动画 | 实现 |
|------|------|------|------|
| 按钮 | hover | 背景色渐变 + 轻微上移 | `hover:-translate-y-0.5 transition-all duration-150 ease-out` |
| 按钮 | active | 轻微缩小 | `active:scale-[0.97]` |
| 功能卡片 | hover | 上浮 + 阴影加深 + 边框亮起 | `hover:-translate-y-1 hover:shadow-md hover:border-amber-400 transition-all duration-200` |
| 表格行 | hover | 背景淡入 | `hover:bg-amber-50/50 transition-colors duration-150` |
| 侧边栏项 | hover | 背景淡入 | `hover:bg-amber-100 transition-colors duration-150` |
| 侧边栏选中 | 切换 | 左边框滑入 | `border-l-4 border-amber-500`（瞬态，非动画） |
| 统计数字 | 页面加载 | 淡入 + 轻微上移 | `animate-fadeInUp`（见下方关键帧） |
| 板块入场 | 滚动可见 | 错位淡入（自上而下 50ms 间隔） | `animate-fadeInUp animation-delay-[Nms]` |
| Badge | 出现 | 缩放淡入 | `animate-scaleIn` |
| 下拉菜单 | 展开 | 淡入 + 向下展开 | `animate-fadeInDown` |
| 弹窗/Modal | 打开 | 缩放淡入 + 背景遮罩淡入 | `animate-scaleFadeIn` |
| 页面切换 | 路由 | 内容区淡入 | `animate-fadeIn duration-200` |

### 自定义关键帧

```css
@keyframes fadeInUp {
  from { opacity: 0; transform: translateY(8px); }
  to   { opacity: 1; transform: translateY(0); }
}

@keyframes scaleIn {
  from { opacity: 0; transform: scale(0.9); }
  to   { opacity: 1; transform: scale(1); }
}

@keyframes fadeInDown {
  from { opacity: 0; transform: translateY(-4px); }
  to   { opacity: 1; transform: translateY(0); }
}

.fade-in-up {
  animation: fadeInUp 0.3s ease-out both;
}

.scale-in {
  animation: scaleIn 0.2s ease-out both;
}

/* 错位入场：子元素依次延迟 */
.stagger-1 { animation-delay: 0ms; }
.stagger-2 { animation-delay: 50ms; }
.stagger-3 { animation-delay: 100ms; }
.stagger-4 { animation-delay: 150ms; }
```

### ❌ 不做的动画

- ❌ 不要旋转、弹跳、脉冲循环（除非加载状态）
- ❌ 不要 parallax 视差滚动
- ❌ 不要粒子/烟花/五彩纸屑
- ❌ 不要文字逐字打字动画
- ❌ 不要让用户等待动画完成才能操作（max 300ms）
- ❌ 不要在同一页面使用 3 种以上不同类型的动画

## Components

### 页面板块（Section Band）

板块是管理后台的基础布局单元。每个主要功能区用一个板块包裹：

```tsx
// 标准板块（白底）
<section className="bg-white rounded-xl p-6 space-y-4">
  <h2 className="text-xl font-bold text-amber-800">学生列表</h2>
  ...
</section>

// 交替板块（浅琥珀底）— 与相邻板块形成视觉区分
<section className="bg-amber-50/80 rounded-xl p-6 space-y-4">
  <h2 className="text-xl font-bold text-amber-800">今日考勤</h2>
  ...
</section>

// 强调板块（深色底）
<section className="bg-[#514A2E] rounded-xl p-6 text-white">
  <h2 className="text-xl font-bold text-amber-400">快捷操作</h2>
  ...
</section>
```

### 统计卡片 (Stat Card)

仪表板的核心组件，展示关键数字：

```tsx
<div className="grid grid-cols-1 md:grid-cols-3 gap-6">
  <div className="bg-white p-6 rounded-xl border border-amber-200/60 shadow-sm relative overflow-hidden">
    <div className="w-1.5 h-12 bg-amber-500 rounded-full absolute left-0 top-6" />
    <p className="text-sm font-medium text-slate-500 ml-3">学生总数</p>
    <p className="text-3xl font-bold text-slate-900 ml-3">128</p>
    <p className="text-xs text-slate-400 ml-3">本月新增 12 人</p>
  </div>
  <!-- 重复 3 列 -->
</div>
```

### 功能导航卡片 (Feature Card)

模块入口卡片（Miro 风格）：

```tsx
<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
  <a className="bg-amber-50 border border-amber-200 rounded-xl p-5 hover:shadow-md transition-all
                hover:border-amber-400 group cursor-pointer">
    <div className="w-10 h-10 bg-amber-200 rounded-lg flex items-center justify-center mb-3
                    group-hover:bg-amber-400 transition-colors">
      <Users className="h-5 w-5 text-amber-800" />
    </div>
    <h3 className="font-semibold text-amber-900">学生管理</h3>
    <p className="text-sm text-amber-700/70 mt-1">查看、添加、编辑学生信息</p>
  </a>
  ...
</div>
```

### 数据表格 (Data Table)

管理后台最常用的组件：

```tsx
<div className="bg-white rounded-xl border border-amber-200/60 overflow-hidden">
  <table className="w-full">
    <thead>
      <tr className="bg-amber-50/80 text-xs uppercase tracking-wider text-amber-800">
        <th className="px-4 py-3 text-left">姓名</th>
        <th className="px-4 py-3 text-left">班级</th>
        <th className="px-4 py-3 text-left">状态</th>
        <th className="px-4 py-3 text-right">操作</th>
      </tr>
    </thead>
    <tbody>
      <tr className="border-t border-amber-100/50 hover:bg-amber-50/50 transition-colors">
        <td className="px-4 py-3">...</td>
      </tr>
    </tbody>
  </table>
</div>
```

### 按钮

| 变体 | Tailwind | 用途 |
|------|----------|------|
| Primary | `bg-amber-600 hover:bg-amber-700 text-white rounded-lg px-4 py-2 font-semibold` | 主要操作 |
| Secondary | `bg-amber-50 border border-amber-200 text-amber-800 rounded-lg px-4 py-2` | 次要操作 |
| Ghost | `text-amber-700 hover:bg-amber-50 rounded-lg px-4 py-2` | 文字按钮 |
| Action | `bg-[#514A2E] text-amber-400 hover:bg-[#3D3822] rounded-lg px-5 py-2.5` | 强调CTA |

### Badge（状态标签）

| 状态 | Tailwind | 示例 |
|------|----------|------|
| ✅ 已批准 | `bg-green-100 text-green-800 rounded-full px-3 py-0.5 text-xs font-semibold` | 学生/教师状态 |
| ⏳ 待审核 | `bg-amber-100 text-amber-800 rounded-full px-3 py-0.5 text-xs font-semibold` | 注册审核 |
| ❌ 已暂停 | `bg-red-100 text-red-800 rounded-full px-3 py-0.5 text-xs font-semibold` | 账户状态 |
| 💰 已付款 | `bg-green-100 text-green-800 rounded-full px-3 py-0.5 text-xs font-semibold` | 费用状态 |
| ⚠️ 逾期 | `bg-red-100 text-red-800 rounded-full px-3 py-0.5 text-xs font-semibold` | 费用状态 |
| 🔶 部分付款 | `bg-amber-100 text-amber-700 rounded-full px-3 py-0.5 text-xs font-semibold` | 费用状态 |

### 侧边栏

```tsx
<aside className="w-64 bg-amber-50/80 border-r border-amber-200/40 min-h-screen">
  <div className="p-4">
    <div className="flex items-center gap-3">
      <div className="p-2 rounded-lg bg-gradient-to-br from-amber-400 to-orange-500">
        <GraduationCap className="h-6 w-6 text-white" />
      </div>
      <span className="font-bold text-amber-900">PJPC</span>
    </div>
  </div>
  <nav className="px-3 space-y-1">
    <a className="flex items-center gap-3 px-3 py-2 rounded-lg text-amber-700 hover:bg-amber-100
                  font-medium transition-colors">
      <LayoutDashboard className="h-5 w-5" />
      仪表板
    </a>
    <!-- 选中态：bg-amber-100/80 border-l-4 border-amber-500 -->
    <a className="flex items-center gap-3 px-3 py-2 rounded-lg bg-amber-100/80
                  text-amber-900 font-semibold border-l-4 border-amber-500 transition-colors">
      <Users className="h-5 w-5" />
      学生管理
    </a>
  </nav>
</aside>
```

### 登录页

```tsx
<div className="min-h-screen bg-gradient-to-br from-amber-50 via-yellow-50 to-orange-50
                flex items-center justify-center p-4">
  <div className="w-full max-w-md">
    <!-- Logo -->
    <div className="text-center mb-6">
      <div className="inline-flex p-3 rounded-full bg-gradient-to-br from-amber-400 to-orange-500 shadow-lg">
        <GraduationCap className="h-8 w-8 text-white" />
      </div>
      <h1 className="text-2xl font-bold text-amber-900 mt-2" style={{ fontFamily: 'Nunito' }}>
        安亲班管理系统
      </h1>
    </div>
    <!-- Card -->
    <div className="bg-white rounded-xl border border-amber-200/60 shadow-xl shadow-amber-200/20 p-6">
      <Tabs defaultValue="login">
        <TabsList className="grid grid-cols-3">
          <TabsTrigger value="login">登录</TabsTrigger>
          <TabsTrigger value="register">注册</TabsTrigger>
          <TabsTrigger value="reset">重置密码</TabsTrigger>
        </TabsList>
        ...
      </Tabs>
    </div>
  </div>
</div>
```

## Do's and Don'ts

### ✅ Do
- 用 **背景色块交替**（白→浅琥珀→深琥珀）区分不同功能板块
- 统计卡片用 **左边竖条色块** 修饰（w-1.5 h-12 bg-amber-500 rounded-full）
- 表格要有 **悬停高亮**（hover:bg-amber-50/50）
- Badge 都用 `rounded-full`，语义色区分
- 标题 Nunito，正文 Inter
- 卡片网格用 `grid-cols-1 md:grid-cols-2 lg:grid-cols-3`
- 模块入口卡片用悬停效果（shadow + 边框变化）
- **图标用 Lucide 简约线条风格**，如一划完成的 GraduationCap、Users、Calendar，避免多层级复杂 SVG 路径
- **Logo 用渐变圆形容器** `bg-gradient-to-br from-amber-400 to-orange-500` + 白色简约图标

### ❌ Don't
- 不要用蓝色/紫色/粉色做主色或强调色
- 不要在深色底上用黑色文字
- 不要让卡片之间没有间距（最小 gap-4）
- 不要在表格里用纯黑边框（用 `border-amber-100/50`）
- 不要忽略移动端（表格可横向滚动）
- 不要在一个页面里有 3 个以上相同的背景色相邻（要交替）
